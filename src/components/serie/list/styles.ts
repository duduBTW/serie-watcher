import styled from "styled-components/native";

// Serie
export const SerieListContainer = styled.View`
  padding: 32px 0 20px;
  border-top-color: ${({ theme }) => theme.color.divider};
  background-color: ${({ theme }) => theme.color.background.main};
  border-top-width: 1px;
`;
